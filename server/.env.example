# Local dev: point this at your local postgres instance
# Format: postgres://USER:PASSWORD@HOST:PORT/DATABASE
DATABASE_URL=postgres://postgres:postgres@localhost:5432/utrgv_match

# In production on Render, set DATABASE_URL to the Internal Database URL
# from your Render Postgres instance (Render sets this automatically if
# you link the DB to the service via render.yaml or the dashboard).
